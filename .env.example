# === StudyPulse configuration (.env.example) ===
# Copy to .env and fill values.

# MySQL
DB_HOST=casestudy
DB_NAME=Study_Pulse
DB_USER=root
DB_PASSWORD=
DB_PORT=3306

# Pusher (client-side + server-side)
PUSHER_APP_ID=2154131
PUSHER_KEY=11bcc45e672f6bd4eb1a
PUSHER_SECRET=39e58f52f4ab363f8ce5
PUSHER_CLUSTER=ap1
PUSHER_USE_TLS=true

# Pusher: used for channel subscription naming (optional)
# Example: create your own channel naming strategy.
PUSHER_DEFAULT_CHANNEL=my-channel

